procs:
  Host:
    shell: pnpm --filter host start --platform ios
    stop: SIGKILL
  Auth:
    shell: pnpm --filter auth start --platform ios
    stop: SIGKILL
  Top Up:
    shell: pnpm --filter topup start --platform ios
    stop: SIGKILL
  Shopping:
    shell: pnpm --filter shopping start --platform ios
    stop: SIGKILL
